import styled from "styled-components";
import { ContainerDashboard } from "@/style/shareStyle";

export const ContainerCreateRequestCommunication = styled(ContainerDashboard)`
  .input__menssage {
    grid-column: 1 / span 3;
  }
`;
