import { ContainerDashboard } from "@/style/shareStyle";
import styled from "styled-components";

export const ContainerData = styled(ContainerDashboard)`
  .user__fields {
    margin-top: 32px;
  }

  .form__buttons {
    padding-top: 40px;
    padding-bottom: 10px;
  }
`;
