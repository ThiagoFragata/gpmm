import { colors } from "@/style/theme";
import styled from "styled-components";

export const ContainerDataBox = styled.section`
  box-shadow: 0px 0px 4px rgba(0, 0, 0, 0.25);
  background-color: ${colors.WHITE};
  border-radius: 32px;
  padding: 32px;
  display: flex;
  flex-direction: column;
  flex: 1;
`;
